stages:
  - pretest
  - build
  - test
  - deploy

image: node:9

pretest:lint:
  stage: pretest
  script:
    - npm install
    - npm run lint
  dependencies: []

pretest:src:
  stage: pretest
  script:
    - npm install
    - npm run test-src
  dependencies: []

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 day

test:dist:
  stage: test
  script:
    - npm install
    - npm run test-dist
  dependencies:
    - build
